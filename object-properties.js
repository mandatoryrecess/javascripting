const food = {  
  types: 'only pizza'  
} 
console.log(food.types);

const favFood = {
  types: 'salmon'
}
console.log(favFood['types']);

const lamp = {
  brightness: 8
}
console.log("the lamp is " + (lamp['brightness']) + " level of brightness");